Use the project venv if needed: `source .venv/bin/activate`

run demo-1:
```
python3 run_best_model.py --log-dir ./logs-demo-1/
```

run demo 2:
```
python3 run_best_model.py
```

train (DQN; saves to `./dqn_carracing_logs/` by default):
```
python3 train.py
```

manual:
```
python3 carracing_obstacles.py
```
